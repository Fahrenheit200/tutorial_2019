cmake_minimum_required(VERSION 3.10)
project(GateSimulator)
aux_source_directory(src SRC_LIST)
include_directories(include)
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ../bin)
add_executable(GSConsole ${SRC_LIST})
